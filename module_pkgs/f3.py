import sys
import f2 # this will import the module which is in current directory

print(sys.path)
print("*"*80)
print(dir(f2))

# in sys.path the first module which is defined is the currect directory path 
